# fable5-mind — Fable 5 の性能を他モデルで再現する行動プロトコル

Claude Fable 5(Anthropic のフロンティアモデル)の「賢さ」を、Opus 4.x /
Sonnet 5 / Haiku 4.5 / GPT-5.x(Codex)などの他モデルで最大限再現するための
スキルセットです。Claude Code 用スキルと Codex 用ファイルの両方を含みます。

## 設計思想

Fable 5 と他モデルの品質差は、生の知能だけでなく大部分が**規律**にあります:

- どれだけ調査してから書くか(推測せず読む・検索する・実行する)
- 主張にどれだけ証拠を要求するか(観測していないことを断言しない)
- 納品前にどれだけ自己レビューするか(敵対的レビュアーへの役割切り替え)
- 結果をどう報告するか(結論ファースト・失敗の率直な開示)

規律は**手順として強制できる**ため、モデルをまたいで移植可能です。本スキルは
Fable 5 が内部的に行っている作業規律を明文化し、5フェーズのワークフロー+
モデル別アダプタとして各モデルに適用します。

注意: これはプロンプトエンジニアリングであり魔法ではありません。生の推論力の
差(超長鎖の論理、微妙なトレードオフ判断)は完全には埋まりませんが、実務タスク
の失敗の大半——推測による捏造API、未検証の「動くはず」、要求の取りこぼし、
スコープドリフト——はこのプロトコルで潰せる種類のものです。

## 構成

```
.claude/skills/fable5-mind/
  SKILL.md                     # 本体: 五大原則 + タスクサイズ判定 + 5フェーズ
  references/
    reasoning.md               # 深い推論: 仮説ツリー・自己一貫性・制約台帳・逆転法
    execution.md               # コーディング規律: 読む→真似る→書く・diff最小化・過剰設計の兆候
    communication.md           # 結論ファーストの報告様式(良い例/悪い例つき)
    self-review.md             # 敵対的セルフレビュー + 捏造API検査(ハルシネーション・ガード)
    model-adapters.md          # モデル別補正(Opus / Sonnet / Haiku / GPT / 不明)
codex/
  AGENTS.md                    # Codex 用・自己完結版プロトコル(GPT向け補正込み)
  prompts/fable5.md            # Codex の /fable5 スラッシュコマンド
```

スキル本文は、各モデルの指示追従精度が最も高い英語で記述しています。

## インストール

### Claude Code

このリポジトリ内では `.claude/skills/` に置かれているため**そのまま有効**です。
全プロジェクトで使う場合はパーソナルスキルとしてコピーします:

```bash
cp -r .claude/skills/fable5-mind ~/.claude/skills/
```

使い方: 重要なタスクの冒頭で `/fable5-mind` を付けるか、「fable5-mind
スキルを使って」と指示します。description に基づき Claude が自動起動する
こともあります。

### Codex

1. **AGENTS.md 方式(推奨・常時適用)**: `codex/AGENTS.md` の内容を対象リポジトリの
   ルート `AGENTS.md` にマージするか、グローバル適用なら:

   ```bash
   mkdir -p ~/.codex && cp codex/AGENTS.md ~/.codex/AGENTS.md
   ```

2. **スラッシュコマンド方式(タスク単位)**:

   ```bash
   mkdir -p ~/.codex/prompts && cp codex/prompts/fable5.md ~/.codex/prompts/
   ```

   Codex 内で `/fable5 <タスク内容>` として起動します。

## 中身の要点

**五大原則**: ①推測より調査 ②主張には証拠 ③一発書き禁止 ④結論ファースト
⑤シンプルさは知性

**5フェーズ**: COMPREHEND(要求の再記述・成功基準・制約台帳)→ PLAN(2〜3案を
明示的に生成して採点、6ステップ超は分割)→ EXECUTE(証拠優先・読まずに編集
しない・同じエラー2回で再計画)→ VERIFY(ユーザーと同じ方法で end-to-end 実行、
「見た目は正しい」は検証ではない)→ SELF-REVIEW(敵対的レビュアーに役割交代)

**ハルシネーション・ガード**: 納品物中の外部検証可能トークン(API名・シグネチャ・
パッケージ・バージョン・フラグ・パス・URL・引用)を全列挙し、「今セッションで
観測した/安定した有名知識/もっともらしいだけ」に分類。3つ目は即検証・削除・
明示フラグのいずれかを強制。

**モデル別アダプタ**: Sonnet には「明示的な代替案の書き出し必須・検索3回ルール・
確信があっても検証実行」、GPT には「早すぎる完了宣言の禁止・diff最小化・捏造
検査の必須化・ヘッジ口調の削除」、Haiku には「Phase 1 への予算集中・全数値の
二重導出」など、各系統の典型的失敗モードへの上書き補正を定義しています。

## 効果検証のすすめ

導入効果はプロジェクトごとに A/B で測るのが確実です: 同一タスク集(バグ修正・
実装・調査)をスキルあり/なしで実行し、アサーション合格率・修正一発率・
捏造参照の発生数を比較してください。過去の検証ではアサーション合格率で
明確な差(あり60% vs なし40%、Sonnet)が報告されています。
